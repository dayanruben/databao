[project]
name = "databao-agent"
description = "databao-agent: NL queries for data"
dynamic = ["version"]  # Based on the git tag
requires-python = ">=3.11, <3.15"
readme = "README.md"
license = "Apache-2.0 AND LicenseRef-Additional-Terms"
license-files = ["LICENSE.md"]
authors = []
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
dependencies = [
    "pandas>=2.2.2",
    "pydantic>=2.8.0,<3",
    "langgraph>=0.6.8",
    "langchain>=1.1.3",
    "langchain-openai>=1.1.3",
    "langchain-anthropic>=1.3.0",
    "langchain-ollama>=1.0.0",
    "duckdb>=1.3.0",
    "jinja2>=3.1.6",
    "tabulate>=0.9.0",
    "psycopg2-binary>=2.9.10",
    "matplotlib>=3.10.0",
    "edaplot-vl~=0.1.4",
    "diskcache>=5.6.3",
    "sqlalchemy>=2.0.45",
    "snowflake-sqlalchemy>=1.8.2",
    "sqlalchemy-bigquery>=1.11.0",
    "databao-context-engine[postgresql,mysql,snowflake]~=0.7.2",
    "mcp>=1.0.0,<2",
    "watchdog>=6.0.0",
    "pandas-stubs~=2.3.3",
    # dbt agent-related deps
    "dbt-core~=1.9.0", # Don't use 1.10 because it outputs a lot of deprecation warnings
    "dbt-duckdb>=1.10.0",
    "claude-agent-sdk>=0.1.48",
    "anyio>=4.11.0",
]

[project.urls]
Homepage = "https://databao.app/"
Source = "https://github.com/JetBrains/databao-agent"

[project.optional-dependencies]
jupyter = [
    "anywidget>=0.9.0",
    "traitlets>=5.0.0",
]
examples = [
    "notebook>=7.4.7",
    "python-dotenv>=1.1.1",
    "anywidget>=0.9.0",
    "traitlets>=5.0.0",
]

[build-system]
requires = ["hatchling", "uv-dynamic-versioning"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "uv-dynamic-versioning"

[tool.uv-dynamic-versioning]
# Use `git tag -a v0.0.0 -m "v0.0.0"` to create a tag.
vcs = "git"
style = "pep440"
fallback-version = "0.0.0"  # https://github.com/ninoseki/uv-dynamic-versioning/blob/main/docs/tips.md
bump = true

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.sdist]
include = ["/databao"]
force-include = { "client/out" = "client/out" }

[tool.hatch.build.targets.wheel]
include = ["/databao"]
force-include = { "client/out" = "client/out" }

[tool.hatch.build.hooks.custom]
path = "hatch_build.py"

[tool.hatch.build.targets.sdist.hooks.custom]

[tool.hatch.build.targets.wheel.hooks.custom]

[dependency-groups]
dev = [
    "isort>=7.0.0",
    "mypy>=1.18.2",
    "pandas-stubs>=2.3.2.250926",
    "pre-commit>=4.3.0",
    "pytest>=7.4.0",
    "ruff>=0.13.2",
    "types-pyyaml>=6.0.12.20250915",
    "typing-extensions>=4.15.0",
    "vega-datasets>=0.9.0",
]

[tool.mypy]
strict = true
show_error_codes = true
check_untyped_defs = true
disallow_untyped_defs = true
warn_redundant_casts = true
warn_unused_configs = true
warn_unused_ignores = true
allow_redefinition_new = true
local_partial_types = true
explicit_package_bases = true
files = ["databao", "tests", "examples"]
# Standalone sub-project with its own pyproject.toml and dependencies (ragas, datacompy);
# its internal imports (benchmark.*, config) only resolve when run from within that directory.
exclude = ["examples/benchmark_template/"]
plugins = ["pydantic.mypy", "sqlalchemy.ext.mypy.plugin"]

[tool.ruff]
line-length = 120
extend-exclude = ["*.ipynb", "databao/agent/databases/database_connection.py"]

[tool.ruff.lint]
select = [
    # pycodestyle
    "E",
    # pycodestyle warnings
    "W",
    # Pyflakes
    "F",
    # pyupgrade
    "UP",
    # flake8-bugbear
    "B",
    # flake8-simplify
    "SIM",
    # isort
    "I",
    # ruff-specific
    "RUF",
    # Prefer absolute imports over relative ones
    "TID252",
]

[tool.ruff.lint.flake8-tidy-imports]
# Disallow all relative imports.
ban-relative-imports = "all"

[tool.uv]
cache-keys = [{ file = "pyproject.toml" }, { git = { commit = true, tags = true }}]

[tool.pytest.ini_options]
addopts = ["--strict-markers"]
markers = [
    "apikey: marks tests that require an API key",
]
